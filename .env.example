# Cross-origin API base URL (optional).
# Leave unset to use the same origin (the bundled Mock Service Worker intercepts
# requests). Set this to point the client at a real backend, and remove the MSW
# bootstrap in src/main.tsx.
# VITE_API_BASE_URL=https://api.example.com
